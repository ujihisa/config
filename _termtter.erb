filter 'english'
plugin 'erb'
plugin 'fib'
plugin 'plugin'
plugin 'uri-open'
#plugin 'spam'

configatron.user_name = 'ujm'
configatron.password = '<%= @termtter[:password] %>'
configatron.update_interval = 60
